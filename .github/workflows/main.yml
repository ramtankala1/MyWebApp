name: A workflow for my Hello World website
on: push
jobs:
  build-job:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Check out ndex code
      run: cat index.html
    - name: Check out courses code
      run: cat courses.html
